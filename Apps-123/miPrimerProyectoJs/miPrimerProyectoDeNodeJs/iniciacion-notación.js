// Notación
// ; => Delimitar el final de una línea
const b = 4;

b + 4;

// .  => Delimitar propiedades de objetos para acceder a los atributos
// movil.anchura

// []  => Delimitar elementos de arreglos, listas o matrices.
const ar = [1, 2, 3, 4, 5];
console.log(ar[2]);

// ()  =>  Funciones
function suma(a, b) {
  //se escribe la función
}

// {} => Delimitar objetos, funciones y estructuras de control
const movil = {
  anchura: 5,
  altura: 10,
};

if (true) {
  // se escribe la condición

  const constante2 = "hola";
}
